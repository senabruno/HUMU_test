{
  "name": "humu-senior-tests",
  "version": "1.0.0",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "cypress run"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/support/step-definitions/**/*.ts",
    "filterSpecs": true,
    "omitFiltered": true,
    "nonGlobalStepDefinitions": false
  }
}